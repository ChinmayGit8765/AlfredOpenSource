# Copy to .env (gitignored) or export in your shell before `alfred run`.
# NEVER commit the real token: it grants full control of your bot.
# Create a bot and get its token at https://discord.com/developers/applications
ALFRED_DISCORD_TOKEN=
